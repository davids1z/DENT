fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn>=22.0.0
httpx==0.28.1
python-multipart==0.0.20
pydantic-settings==2.7.1
# Forensic analysis dependencies
Pillow>=10.4.0
numpy>=1.26.0
python-magic>=0.4.27
PyExifTool>=0.5.6
c2pa-python>=0.6.0
# Fallback EXIF reader (used when ExifTool binary is unavailable)
exifread>=3.0.0
# Phase 2: Enhanced ELA & CNN forensics
opencv-python-headless>=4.9.0
scipy>=1.12.0
# Phase 3: Optical forensics (Moire / wavelet analysis)
PyWavelets>=1.5.0
# Phase 5: Document forensics (PDF analysis)
pypdf>=4.0.0
pyhanko>=0.25.0
cryptography>=42.0.0
# Advanced PDF forensics (fake redaction, shadow attack detection)
PyMuPDF>=1.24.0
# Office document forensics (VBA macro detection)
oletools>=0.60.0
# Phase 8: Evidence standardization (PDF report generation)
fpdf2>=2.8.0
# CNN deep learning (optional — graceful degradation if missing)
# torch and photoholmes are installed separately in Dockerfile for layer caching
# AI generation detection (optional — transformers installed in Dockerfile for layer caching)
# Models: Organika/sdxl-detector, umm-maybe/AI-image-detector
# CLIP AI detection (UniversalFakeDetect style — model downloaded via transformers)
# VAE reconstruction error detection (DIRE/CO-SPY style)
diffusers>=0.27.0
# Community Forensics ViT-Small (CVPR 2025, 4803 generators)
timm>=1.0.0
safetensors>=0.4.0
# Content validation (OCR for scanned documents)
pytesseract>=0.3.10
# PDF structural forensics (font glyph analysis — optional, graceful degradation)
fonttools>=4.40.0
# QR code reading for Croatian invoice validation (optional, graceful degradation)
pyzbar>=0.1.9
# ONNX Runtime for accelerated inference (2-3x faster than PyTorch on CPU)
onnxruntime>=1.17.0
